# Siddharth Singh — Portfolio

Static, zero-build portfolio with a modern, glassmorphic aesthetic.

## Run locally
```bash
cd /Users/siddharthsingh/Desktop/Code/VibeCoded/site
python3 -m http.server 3000
# then open http://localhost:3000
```

You can also open `index.html` directly in the browser without a server.

## Customize
- Edit `index.html` for content/sections.
- Tweak visuals in `styles.css` (colors, spacing, gradients).
- Adjust animations/theme toggling in `script.js`.

